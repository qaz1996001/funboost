Demonstrates the usage of the framework, specifically the RPC feature.

The RPC feature is very useful for cross-process, cross-Python-interpreter, cross-server, and cross-language scenarios.

Unless the interface is directly facing the frontend, it is not mandatory. It can be changed to RPC to reduce interface code and improve performance.
