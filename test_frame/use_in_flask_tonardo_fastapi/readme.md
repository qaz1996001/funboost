## Demonstrates integration with a web framework: publishing tasks to the middleware via a web interface, with a separate background consumer started independently.


## If the frontend needs to be aware of the consumption result, the best approach is MQTT, which is better than both Ajax polling and WebSocket.
