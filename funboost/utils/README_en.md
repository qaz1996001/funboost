# The utils functionality here was copied from an existing project, and most of it is not used, so don't worry about this directory.

# The most important thing is to look at base_consumer.py, where the framework's 90% logic flow is in AbstractConsumer
